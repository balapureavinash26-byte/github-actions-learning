name: Lesson 8 - Reusable Workflow

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:

  call-reusable-workflow:
    name: Call Reusable CI Workflow

    uses: ./.github/workflows/reusable-ci.yml

    with:
      app_name: Payroll Application

    secrets:
      demo_secret: ${{ secrets.LESSON8_SECRET }}
